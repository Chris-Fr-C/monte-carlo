# Description
Create under src/backtesting the required functions to evaluate the impact of each signal into the quote (stock price).
It should generate:
- A structured output (struct) per signal.
- A structured output (struct) for the strategy.

It should be configurable with an input struct that would have
the fee cost (flat or %), initial money,  etc ...

# Validation

- Functions are written and tested. Use hardcoded data in the unit test with around 10 different points. Not more.



# Depends on

- strategy-implem.md
# Comments
